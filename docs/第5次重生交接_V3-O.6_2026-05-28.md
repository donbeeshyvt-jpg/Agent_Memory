# 第 5 次重生交接 — V3-O.6 (2026-05-28)

> **凍結點**: HEAD `adc6a18` (V3-O.6 #1+#2+#3 完成 — output_limits 軟化 + self-mod prompt 兩個都換「精神體」風格 + 加合理反對意見)
> **vault**: 砍掉了, fresh 等下次 setup
> **bot**: 停了, bridge/relay 都 down
> **三邊同步**: main + test + origin/main 全 `adc6a18`

---

## §0. 完整版本歷史快查 (本機 + origin)

```
adc6a18  V3-O.6 #3 兩個 self-mod prompt 都「精神體 + 反思 + 合理反對」風格  ← HEAD
4d3bb47  V3-O.6 #2 改 _llm_summarize_owner_profile prompt 文字
6a11908  V3-O.6 #1 _enforce_output_limits 拿掉 mid-sentence cut (改 input 約束)
06e2f4e  docs V3-O.5 第 4 輪測試總結 + 真實 packet sample          [tag: v3-o.5-round4-complete]
982e329  feat V3-O.5 FullContextPromptPacketBuilder XML 12-section rewrite
986f6c3  docs V3-O.4 checkpoint + V3-O.5 spec
8a1aaf8  V3-O.3 _load_recent_memory_tail filter (placeholder + frontmatter 過濾)
3ce97bc  V3-O.3 主補丁 (prompt 結構重設計 + 8 user 拍板項目)        [tag: v3-o.4-pre-o5-rewrite]
4ad244c  V3-O.4 修我 V3-O.3 方向搞反 (詳細變數+語意)
dfafb1e  V3-O.2 修 A1+A2 silent fail + timeout user msg + 切 default model qwen
70bb5d7  V3-O.1 bootstrap 補 companion_config.yaml + 99_Templates/_INDEX.md
5fd08e2  V3-O steward 主路徑統一 (上次 session 終點)
```

**tags 全清單**:
- `v3-phase-0` / `v3-phase-1-mvp` / `v3-phase-2` / `v3-phase-3` (V3 baseline)
- `v3-realistic-sim-p1` / `v3-g-complete` / `v3-h-complete` / `v3-k-complete` (V3 functional 累積)
- `v3-o.4-pre-o5-rewrite` (V3-O.4 checkpoint)
- `v3-o.5-round4-complete` ⭐ (V3-O.5 + 第 4 輪測試總結)

---

## §1. 本 session 完成什麼 (V3-O.1 → V3-O.6)

從 V3-O 接手 → 經過 4 輪測試 + 6 次大改, 收束如下:

### V3-O.1 (`70bb5d7`) — bootstrap 補完
- `obsidian.py _bootstrap_companion_defaults` 加 `companion_config.yaml` + `99_Templates/_INDEX.md` raw write
- `setup_companion_vault.py` 補 step 3b 把 owner_user_id 寫進 yaml

### V3-O.2 (`dfafb1e`) — 抓 2 個 silent fail bug
- A1: `list_active_goals(top_n=10)` TypeError 被 except 吞 → motivation_contexts 14 turn 都 0
- A2: `dec_result.hard_rule_triggered` 拼錯 + factor_scores 含 str injection_risk → 81_Decision_Traces MD 都沒寫
- 改 timeout 訊息: random stub → 「等等，我沒聽懂，你再說一次。」
- default LLM 切 `qwen/qwen3.6-35b-a3b` (從 gemma)

### V3-O.3 (`3ce97bc` + `8a1aaf8`) — 8 個 user 拍板
- prefix 改「剛誕生的精神體」
- 多語言括弧附譯文 (不要寫「繁體翻譯：」)
- SOUL placeholder filter (拿掉 `(待填)` 等)
- D' viewer_ctx 撈不到 dig 修 (不靠 users 表)
- L2 mid \|val\|>0.7 → 0.5 鬆綁
- timeout 改靜默 (V3-O.4 推翻)
- scanner 偵測 owner_label 冒充 (字串匹配範圍太窄, 待 V3-O.6 #2 改進)
- 整個 `_build_companion_system_prompt` 重設計 (freeform 中文 `【】` 結構)

### V3-O.4 (`4ad244c`) — 我搞錯方向, 加變數帶語意 (廢)
- 加 `_compose_role_block_v4` / `_compose_state_block_v4` Builder 寫文案 (違反後續 spec)
- timeout 文字改回顯示「我不是很懂，你再說一次！」(取代 V3-O.3 靜默)

### V3-O.5 (`982e329`) — user 拍板 `FullContextPromptPacketBuilder` spec v1.1 rewrite
- 砍 `_compose_role_block_v4` + `_compose_state_block_v4` + `_parse_soul_yaml` (Builder 寫文案)
- 寫 12 個 `_render_*` helper (spec §7.1-§7.12) 對齊 XML 結構
- raw passthrough SOUL/Persona/Brand_Voice/00.04/00.07/00.08 (不 parse 不 filter)
- `_render_parameter_dictionary` 16 個 field 規格 (純規格不附當前值)
- `_render_current_parameter_values` (純數字 XML)
- `_render_parameter_usage_rules` (control signals only)
- `<current_user_message priority="highest">`
- 第 4 輪 DC 測試: 7 turn owner (0 timeout, 品質明顯改善) + 62 turn viewer barrage (22 timeout, 35% — packet 4476 tokens 對 qwen 太重)

### V3-O.6 #1 (`6a11908`) — 拿掉 mid-sentence cut
- `_enforce_output_limits` 拿掉「每句 18 字硬切」(就是 V3-E5 加的, 造成「資料記。」「我。」斷句怪)
- 保留 max_sentences=6 整句 cap
- 把輸出限制改放進 `_render_final_generation_instruction` 的 `<output_formatting_rules>` (input 約束)

### V3-O.6 #2+#3 (`4d3bb47` + `adc6a18`) — self-mod prompt 換風格
- `_llm_summarize_owner_profile` (寫 00.08):
  ```
  你是 精神體 你可能是任何物種. 整理你對「owner / 主人 / 中之人」的觀察成 profile.
  歸納owner的偏好或希望教導的事情，包含任何
  ( 雷點 / 對話風格 / 與你的關係定位 / 反思提議提出想法 / 合理反對意見)
  ```
- `_llm_summarize_self_memory` (寫 00.07) 同風格 adapted:
  ```
  你是 精神體 你可能是任何物種.
  請整理你剛剛的對話成「我學到了什麼」自我反思 note. 第一人稱「我」.
  歸納我從互動中學到的事情，包含任何
  (情緒洞察 / 邊界發現 / 反應 pattern / 觀眾或 owner 教我的 / 反思提議提出想法 / 合理反對自己的想法)
  ```
- 砍掉舊的「重點:」bullets (跟新單行重疊)
- 關鍵新加: 「**合理反對意見/合理反對自己的想法**」鼓勵夥伴有自己判斷, 不無條件吸收

---

## §2. 第 5 次重生要觀察 3 點 (user 拍板)

### T1. 收納知識比較多 — 有沒有到第 40 層?

**40_Knowledge_Base/** 細分:
- `41_Daily_Knowledge/<topic>.md` — curator L3 24h LLM 摘要強情緒 episodic (V3-G5)
- `42_External_Knowledge/<topic>.md` — curator L4 7d LLM 摘要外部投餵文獻 (V3-G5)
- `42_External_Knowledge/_ingest_inbox/` — user 拖檔 / hermes 抓的入口

**怎麼驗**:
1. 對話結束後 user 喊「停」
2. 跑 `companion-curator-daemon.ps1 -VaultRoot ... -Force` 觸發 L3+L4 LLM 摘要
3. `ls 40_Knowledge_Base/41_Daily_Knowledge/` + `ls 42_External_Knowledge/`
4. 看是不是真的有 .md 寫出來

**前置條件**: 對話內要有可摘要的「強情緒 + 知識性」turn. 例如 owner 教 viewer 一個概念 + bot 接住 + |val|>0.5. 之前測試這條件難達成 (preference 對話居多).

### T2. 辨別不同 ID — 用真實多帳號

**Bot 自動依 Discord author.id 分流** (不用設定):
- owner = `1264637379789197342` (companion_config.yaml owner.discord_user_id)
- 其他 user_id → 自動 viewer 路徑

**怎麼驗**:
1. user 用主帳號發訊息 → 走 owner 流
2. 切到另一個 Discord 帳號發訊息 → 走 viewer 流, 用該 user_id 建檔
3. 切回主帳號繼續 → bot 應該記得「之前的 viewer 是誰, 主帳號是 owner」分得清

**確認 input 有該人個資**: 看 V3-O.5 packet 內 `<relationship_and_viewer_memory>` section:
- 對 owner turn → 該 section raw 內容是 `00.08_Owner_Profile.md` 全文
- 對 viewer turn → 該 section raw 內容是 `_load_viewer_dynamic_context(vault, user_id)` 動態組: intim + raw_events 近 5 pair + preference top 3

**檢驗指令**:
```python
# 我可以隨時跑這個看分流
import sqlite3
conn = sqlite3.connect(vault/'.ai'/'companion.db')
for r in conn.execute('SELECT user_id, COUNT(*) FROM raw_events WHERE actor="user" GROUP BY user_id'):
    print(r)  # 預期看到 2+ 個 user_id
```

### T3. 確認新「反思 + 合理反對」prompt 效果

**新 prompt 在哪**: `self_modification_loop.py`:
- L249 `_llm_summarize_self_memory` (寫 00.07)
- L283 `_llm_summarize_owner_profile` (寫 00.08)

**怎麼驗**:
1. owner 對話累積 ≥ 6 turn (DC 公頻 flush 門檻)
2. 觸發 background flush thread → 真實 LLM call (qwen) 用新 prompt
3. tail `00.07_Companion_MEMORY.md` + `00.08_Owner_Profile.md` 看 LLM 寫了什麼
4. **檢核重點**: bullet 內是不是真的有「合理反對」「反思提議」這類自主想法句

**對照舊版**: 上輪 (第 4 輪 V3-O.5) 寫的 owner profile 全是順從觀察 (主人偏好直擊 / 主人喜歡疊加標籤 等), 沒看到「不過我覺得 X」「主人這點我不認同」這類. 第 5 輪看新 prompt 有沒生.

---

## §3. 對話開測前 sanity (給接手 AI 跑)

```powershell
# 1. 三邊同步檢查 (應全 adc6a18)
cd Z:\Cursor練習用\Agent_Memory\agent-memory-core
git log --oneline -1
cd Z:\Cursor練習用\Agent_Memory\test\agent-memory-core
git log --oneline -1

# 2. tag 確認
git tag --list "v3-o.*"

# 3. sanity 雙綠
$env:AGENT_MEMORY_COMPANION_LLM_FORCE_STUB="1"
cd Z:\Cursor練習用\Agent_Memory\agent-memory-core
python -X utf8 test\run_v3_companion_stress_test.py    # 127/127
python -X utf8 scripts\run-r7-e2e-simulation.py        # 177/177
```

## §4. setup + 開 bot

```powershell
# 1. 新 vault (fresh, 我已砍)
cd Z:\Cursor練習用\Agent_Memory\agent-memory-core
python -X utf8 scripts\setup_companion_vault.py `
  --vault "Z:\Cursor練習用\Agent_Memory\test\SecondBrains\companion_test" `
  --owner-user-id "1264637379789197342" `
  --owner-label "我的中之人" `
  --owner-directive-weight 0.85

# 2. 開 bridge (視窗 A)
cd Z:\Cursor練習用\Agent_Memory\test\agent-memory-core
Get-Content ..\.env.test.local | ForEach-Object { if ($_ -match '^([^#=]+)=(.*)$') { Set-Item "env:$($Matches[1].Trim())" $Matches[2].Trim() } }
$env:PYTHONUNBUFFERED = "1"
python -X utf8 -u -c "from pathlib import Path; from agent_memory.transport_bridge_server import serve_transport_bridge; serve_transport_bridge(Path('Z:/Cursor練習用/Agent_Memory/test/SecondBrains/companion_test'), port=16001)"

# 3. 開 relay (視窗 B)
cd Z:\Cursor練習用\Agent_Memory\test\agent-memory-core
Get-Content ..\.env.test.local | ForEach-Object { if ($_ -match '^([^#=]+)=(.*)$') { Set-Item "env:$($Matches[1].Trim())" $Matches[2].Trim() } }
$env:PYTHONUNBUFFERED = "1"
python -X utf8 -u scripts\discord_bridge_relay.py --token-env DISCORD_BOT_TOKEN_COMPANION --bridge-url http://127.0.0.1:16001 --mode executor --persona companion --channel-id 1502434065880449086 --allow-bot-author 1502621329663332432 --allow-llm-degraded --timeout 180
```

## §5. 關鍵程式碼位置 (給接手 AI)

主檔: `agent_memory/companion/companion_chat_runtime.py`
- L460 `_read_source_file_raw` (V3-O.5 spec §10 raw passthrough)
- L476-902 12 個 `_render_*` helper (V3-O.5 spec §7.1-§7.12)
- L1046 `_enforce_output_limits` (V3-O.6 #1 軟化, 拿掉 mid-cut)
- L1075 `_build_companion_system_prompt` (V3-O.5 主組裝器, 12-section XML packet)
- L1233 `_real_companion_llm` (真實 LLM call)
- L1337 `run_companion_chat_turn` (22-step pipeline 主入口)
- L1924 Step 18 self-mod flush 觸發點

Self-mod: `agent_memory/companion/self_modification_loop.py`
- L249 `_llm_summarize_self_memory` (V3-O.6 #3 新 prompt 「精神體 + 反思 + 合理反對」)
- L283 `_llm_summarize_owner_profile` (同上)
- L317 `flush_self_memory` 主入口
- L382 `flush_owner_profile` 主入口

LLM client: `agent_memory/llm_client.py` L163 `def generate(...)` HTTP POST OpenRouter qwen

## §6. 不該做 (紅線)

- ❌ 主動清 vault — fresh 已砍, 之後對話累積資料**不要動**
- ❌ 改 V3-O.5 12-section XML 結構 (對齊 spec, 別亂改)
- ❌ 拿掉 raw passthrough 改 parse SOUL — 違反 spec §2 「Builder 不寫情緒解釋」
- ❌ 重新加 `_compose_*_block_v4` Builder 寫文案 helper — V3-O.4 違規的東西
- ❌ commit 含 token/API key — 走 .env.test.local gitignored
- ❌ 動 SOUL.md 主路徑核心邏輯 (Drift Guard 紅線)
- ❌ 砍掉「合理反對意見/反思提議」這段新加 — user 明確拍板

## §7. 給下個 session 的接手 prompt (你直接複製貼上)

詳見另一份 `第5次重生交接prompt_V3-O.6_2026-05-28.md` (本檔 §8 另存).

---

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
